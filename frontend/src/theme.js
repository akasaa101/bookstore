import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#f2b705',
    },
    secondary: {
      main: '#8EC7D2',
    },
    error: {
      main: '#DB073D',
    },
  },
});

export default theme;